import React from "react";
import { View } from "react-native";

// Componente menu a tendina stile iOS (segnaposto)
// Qui andrà il componente dropdown personalizzato per iOS
export default function MenuATendinaIOS() {
  return <View />;
}
