import React from "react";
import { View } from "react-native";

// Componente elemento richiesta nella lista (segnaposto)
// Qui andrà il componente che mostra una singola richiesta con stato e azioni
export default function ElementoRichiesta() {
  return <View />;
}
