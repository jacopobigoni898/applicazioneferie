import React from "react";
import { View } from "react-native";

// Modale per la modifica delle richieste (segnaposto)
// Qui andrà la modale con il form per modificare una richiesta esistente
export default function ModaleModificaRichiesta() {
  return <View />;
}
