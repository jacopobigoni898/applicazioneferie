import React from "react";
import { View } from "react-native";

// Modale per la creazione di nuove richieste (segnaposto)
// Qui andrà la modale con il form per creare una nuova richiesta
export default function ModaleRichiesta() {
  return <View />;
}
