// Mapper per le richieste di permesso (segnaposto)
// Qui andrà la logica per mappare la risposta API all'entità RichiestaPermesso

export const mappaRispostaPermesso = (_dati: any) => {
  // Da implementare
  return null;
};
