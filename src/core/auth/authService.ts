// Servizio di autenticazione Microsoft (segnaposto)
// Qui andrà la logica di login e refresh token tramite MSAL

// Segnaposto: login interattivo con Microsoft
export const accediConMicrosoft = async () => {
  // Da implementare
  return null;
};

// Segnaposto: refresh silenzioso del token Microsoft
export const aggiornaTokenMicrosoft = async (_tokenAggiornamento: string) => {
  // Da implementare
  return null;
};
